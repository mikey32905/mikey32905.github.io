//1.  can't define a variable more than once.
let petName = 'Baby'
petName = 'Spot'
console.log(petName)

//2. Rules for naming variables.  Needs to start with _, $, or letter (a-z).
//  Cannot start with number.

//3. variable names cannot be reserve keywords.  for example, Let.

