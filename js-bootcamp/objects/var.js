//not an ideal way of identifying variables

var firstName = 'Mike'

/*
   var allows for creating, reassigning, and identifying on the fly.  
   var is functioned scope, not  block scoped (let)
   var allows accessing a variable without declaring it.
*/
